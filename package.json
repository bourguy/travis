{
  "name": "travis-ci",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "mocha --exit"
    "if [[ ${NODE_ENV} == \"dev\" ]]; then mocha; else mocha --exit"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.2.0",
    "ejs": "^2.6.1",
    "express": "^4.16.3",
    "mocha": "^5.2.0"
  }
}
